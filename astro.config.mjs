// @ts-check
import { defineConfig } from 'astro/config';

// NOTE: `site` is the canonical public URL. It is a PLACEHOLDER until the real
// Munin domain is registered (candidates: muninjournal.app / getmunin.app).
// Change it in this one place once the domain is locked.
export default defineConfig({
  site: 'https://muninjournal.app',
});
